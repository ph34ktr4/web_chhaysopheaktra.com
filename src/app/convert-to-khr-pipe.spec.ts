import { ConvertToKhrPipe } from './convert-to-khr-pipe';

describe('ConvertToKhrPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertToKhrPipe();
    expect(pipe).toBeTruthy();
  });
});
